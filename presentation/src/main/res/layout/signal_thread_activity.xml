<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/windowBackground"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        style="@style/Toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- The title, with the rail beside it. On a phone where a contact can hold both an
             SMS and a Signal thread, which one you are typing into is not a detail. -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <com.wanderwildwood.kotozute.common.widget.QkTextView
                android:id="@+id/toolbarTitle"
                style="@style/ToolbarText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textDirection="ltr"
                android:textSize="16sp" />

            <com.wanderwildwood.kotozute.common.widget.QkTextView
                android:id="@+id/railBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:background="@drawable/filter_tab_selected"
                android:paddingStart="6dp"
                android:paddingTop="1dp"
                android:paddingEnd="6dp"
                android:paddingBottom="1dp"
                android:text="@string/signal_rail_label"
                android:textColor="@android:color/white"
                android:textSize="11sp"
                android:textStyle="bold" />

        </LinearLayout>

    </androidx.appcompat.widget.Toolbar>

    <View
        android:id="@+id/titleBarBlackLine"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:background="@android:color/black" />

    <!-- Find in conversation. Filters the thread to the messages that match rather than
         stepping through them one at a time: on e-ink, seeing every hit at once costs one
         redraw where stepping costs one per hit. -->
    <LinearLayout
        android:id="@+id/searchBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        android:paddingEnd="8dp"
        android:paddingBottom="8dp"
        android:visibility="gone">

        <com.wanderwildwood.kotozute.common.widget.QkEditText
            android:id="@+id/searchField"
            style="@style/TextPrimary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/rounded_rectangle_22dp"
            android:hint="@string/signal_find_hint"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:maxLines="1"
            android:minHeight="40dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:textColorHint="?android:attr/textColorTertiary" />

        <com.wanderwildwood.kotozute.common.widget.QkTextView
            android:id="@+id/searchCount"
            style="@style/TextSecondary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:minWidth="44dp"
            android:gravity="center" />

        <ImageView
            android:id="@+id/searchClose"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/signal_find_close"
            android:padding="8dp"
            android:src="@drawable/ic_clear_black_24dp"
            android:tint="@android:color/black" />

    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingTop="8dp"
            android:clipToPadding="false" />

        <!-- A thread reached from the directory has no messages in it yet. Blank white with
             a composer under it reads as broken; one line reads as new. -->
        <com.wanderwildwood.kotozute.common.widget.QkTextView
            android:id="@+id/empty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:paddingStart="32dp"
            android:paddingEnd="32dp"
            android:text="@string/signal_thread_empty"
            android:textColor="?android:attr/textColorTertiary"
            android:visibility="gone" />

    </FrameLayout>

    <!-- The message a reply will quote, shaped like the attachment line below it: what it
         is, and a tap takes it away. Two lines at most; the quote itself goes whole. -->
    <com.wanderwildwood.kotozute.common.widget.QkTextView
        android:id="@+id/replying"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:ellipsize="end"
        android:maxLines="2"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        android:paddingEnd="12dp"
        android:paddingBottom="8dp"
        android:textSize="12sp"
        android:visibility="gone" />

    <com.wanderwildwood.kotozute.common.widget.QkTextView
        android:id="@+id/pending"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        android:paddingEnd="12dp"
        android:paddingBottom="8dp"
        android:textSize="12sp"
        android:visibility="gone" />

    <com.wanderwildwood.kotozute.common.widget.QkTextView
        android:id="@+id/cannotSend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:textSize="12sp"
        android:visibility="gone" />

    <View
        android:id="@+id/composeDivider"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_marginBottom="12dp"
        android:background="@android:color/black" />

    <LinearLayout
        android:id="@+id/composer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:gravity="bottom"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/attach"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="12dp"
            android:layout_marginBottom="4dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/compose_attach_cd"
            android:padding="8dp"
            android:src="@drawable/ic_add_black_24dp"
            android:tint="@android:color/black" />

        <com.wanderwildwood.kotozute.common.widget.QkEditText
            android:id="@+id/message"
            style="@style/TextPrimary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:background="@drawable/rounded_rectangle_22dp"
            android:backgroundTint="?attr/bubbleColor"
            android:cursorVisible="false"
            android:gravity="center_vertical"
            android:hint="@string/compose_hint"
            android:inputType="textCapSentences|textMultiLine"
            android:maxLines="6"
            android:minHeight="44dp"
            android:paddingStart="16dp"
            android:paddingTop="12dp"
            android:paddingEnd="16dp"
            android:paddingBottom="12dp"
            android:textColorHint="?android:attr/textColorTertiary" />

        <!--
            Send and the microphone share one place at the end of the row, as they do in the
            SMS composer: the microphone while the box is empty, Send once there is something
            to send. Tap to start recording, tap again to stop. Not press-and-hold: on a panel
            that redraws in its own time there is no feedback fast enough to tell somebody
            their finger is being registered, and a hold that is not registered produces
            silence rather than an error.
        -->
        <FrameLayout
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginEnd="8dp">

            <ImageView
                android:id="@+id/record"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/signal_record_cd"
                android:padding="8dp"
                android:src="@android:drawable/ic_btn_speak_now"
                android:tint="@android:color/black" />

            <ImageView
                android:id="@+id/send"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="@drawable/rounded_square_outline_send"
                android:contentDescription="@string/compose_send_cd"
                android:padding="10dp"
                android:src="@drawable/ic_send_black_24dp"
                android:tint="@android:color/black"
                android:visibility="invisible" />

        </FrameLayout>

    </LinearLayout>

</LinearLayout>
